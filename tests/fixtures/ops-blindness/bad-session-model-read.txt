// @path: src/lib/ops/reads.ts
// @expect: OPS-MODEL-METHOD
// Session and MagicToken hold live sign-in credentials. Not even a count, which
// could confirm a specific row.
import "server-only";
import { db } from "@/lib/db";

export async function liveSessions() {
  return db.session.count();
}
