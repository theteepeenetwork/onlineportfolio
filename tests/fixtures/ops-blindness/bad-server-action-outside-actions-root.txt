// @path: src/app/ops/mutations.ts
// @expect: OPS-REQUIRE-OPERATOR
// The guard check keyed off the directory src/app/actions/ops/ and the route
// file basenames. A "use server" module anywhere else under the ops roots was
// never guard-checked at all.
"use server";

import { freezeSchool } from "@/lib/ops/operations";

export async function freeze(schoolId, reason) {
  return freezeSchool(schoolId, reason);
}
