// @path: src/lib/ops/session.ts
// @expect: OPS-SESSION-REUSE
import { createSession, COOKIE_NAME } from "@/lib/auth";

export async function startOperatorSession(teacherId) {
  console.log(COOKIE_NAME);
  return createSession(teacherId);
}
